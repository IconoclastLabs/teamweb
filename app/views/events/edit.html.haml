%h1 Editing event

= render 'form'

.btn-group
  = link_to icon_tag("hand-left") + 'Back', coordinator_group_events_path, class: "btn btn-primary"
  = link_to icon_tag("play") + 'Show', coordinator_group_event_path(@event.coordinator_group, @event), class: "btn btn-info"
  
