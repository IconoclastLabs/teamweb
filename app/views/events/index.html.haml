%h1 
  = @coordinator.name + pluralize(@events.count, 'Event')

%table.table.table-striped
  %thead
    %tr
      %th Name
      %th CoordinatorGroup
      %th Location
      %th Start
      %th

  - @events.each do |event|
    %tr
      %td= event.name
      %td= event.coordinator_group.name
      %td= event.location
      %td= event.start.try(:format_like, "Jan 1st")
      %td.btn-group
        = link_to icon_tag("play") + 'Show', coordinator_group_event_path(event.coordinator_group, event), class: "btn btn-info"
        = link_to icon_tag("flag") + 'Teams', coordinator_group_event_teams_path(event.coordinator_group, event), class: "btn btn-info"
        - if user_signed_in?
          = link_to icon_tag("wrench") + 'Edit', edit_coordinator_group_event_path(event.coordinator_group, event), class: "btn btn-info"
          = link_to icon_tag("trash") + 'Delete', coordinator_group_event_path(event.coordinator_group, event), :method => :delete, :data => { :confirm => 'Are you sure?' }, :class => "btn btn-danger"

%br

= link_to icon_tag("star-empty") + 'New Event', new_coordinator_group_event_path, class: "btn btn-success" if user_signed_in?
