.navbar
  .navbar-inner
    .container
      %ul.nav
        = nav_link "Home", root_path
        = nav_link "Create Event", new_event_path
        = nav_link "Events", events_path
        = nav_link "Organizations", organizations_path
        - if user_signed_in?
          = nav_link "Profile", edit_user_registration_path
          = nav_link "Logout", destroy_user_session_path, method: :delete
        - else
          = nav_link "Sign Up", new_user_registration_path
          = nav_link "Login", new_user_session_path
/ /.navbar
/ Breadcrumbs via helper
= render_breadcrumbs