%p
  %b Name:
  = @team.name
%p
  %b Event:
  = @team.event.name

.btn-group
  = link_to icon_tag("hand-left") + 'Back', organization_event_teams_path, class: "btn btn-primary"
  = link_to icon_tag("wrench") + 'Edit', edit_organization_event_team_path(@organization, @event, @team), class: "btn btn-info"
  