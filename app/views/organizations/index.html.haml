%h1 Organizations

%table.table.table-striped
  %thead
    %tr
      %th Name
      %th Location
      %th Contact
      %th

  - @organizations.each do |org|
    %tr
      %td= org.name
      %td= org.location
      %td= org.contact
      = render partial: 'buttons', locals: {org: org}

%br

= link_to icon_tag("star-empty") + 'New Organization', new_organization_path, class: "btn btn-success" if user_signed_in?
